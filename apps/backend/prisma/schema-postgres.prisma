generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma-pg/client"
  engineType = "client"
}

datasource db {
  provider = "postgresql"
}

model User { 
  id    Int     @id @default(autoincrement()) 
  email String  @unique
  name  String?
} 